server:
  port: 8383

spring:
  config:
    import: "optional:configserver:"
    application:
      name: gateway-service
    cloud:
      config:
        discovery:
          serviceId: config-service
          enabled: true